import React, { Fragment } from "react";
import { Box } from "@mui/system";

const paws = () => {
  return (
    <Fragment>
      <Box
        component="img"
        alt="Faint cat paws on the backround."
        src="./assets/SVG/paws.svg"
      />
    </Fragment>
  );
};

export default paws;
